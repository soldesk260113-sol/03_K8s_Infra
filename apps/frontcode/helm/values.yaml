# Default values for frontcode.
# This is a minimal values file since we are using raw manifests in templates.
replicaCount: 3

image:
  repository: 10.2.2.40:5000/library/frontcode
  pullPolicy: Always
  tag: "latest-ai-v2"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
